<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" type="image/png" href="images/essu-logo.png">
    <link rel="stylesheet" href="css/forgot-password.css">
    
    <title>Forgot Password</title>
</head>
<body>
    <header class="header">
        <div class="logo">
            <img src="images/logo.png" alt="Logo">
        </div>
        <div class="title">
            <h2>Attendance Monitoring System</h2>
        </div>
    </header>
    <div class="background">
        <img src="images/background.webp" alt="Background Image">
    </div>
    <% if (typeof error !== 'undefined' && error) { %>
        <div class="error-box">
            <%= error %>
        </div>
    <% } %>
 
    <div class="forgot-password-container">
        <h1>Forgot Password</h1>
        <p>Enter your recovery email to receive a reset token.</p>

        <form action="/forgot-password" method="POST">
            <div class="email-input">
                <span class="close">&times;</span>
                <input type="text" name="recovery_email" placeholder="Enter your recovery email" required>
            </div>
            <br>
            <button type="submit">Send Reset Token</button>
        </form>
    </div>

    <!-- JavaScript to handle closing the modal and redirecting to login page -->
    <script>
        // Get the close button
        var closeButton = document.querySelector(".close");

        // Add click event listener to the close button
        closeButton.addEventListener("click", function() {
            // Redirect to the login page
            window.location.href = "/";
        });
    </script>
</body>
</html>
