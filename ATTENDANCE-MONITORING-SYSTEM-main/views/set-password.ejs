<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/reset-password.css">
    <title>Password Recovery</title>
</head>
<body>

    <header class="header">
        <div class="logo">
            <img src="images/logo.png" alt="Logo">
        </div>
        <div class="title">
            <h2>Attendance Monitoring System</h2>
        </div>
    </header>
    <div class="background">
        <img src="images/background.webp" alt="Background Image">
    </div>
    <% if (message) { %>
        <div class="message-box">
            <%= message %>
        </div>
    <% } %>
   
    <div class="forgot-password-container">
        <form id="passwordForm" action="/set-password" method="POST">
            <h2 style="text-align: center;">Reset Password</h2>

            <% if (messages.error) { %>
                <li style="text-align: center; color: rgb(255, 0, 0); font-size: 18px; font-family: 'Times New Roman', Times, serif;"><%= messages.error %></li>
            <% } %>
            
            <input type="hidden" name="token" value="<%= token %>">
            <label for="newPassword">New Password:</label>
            <input type="password" id="newPassword" name="new_password"  placeholder="Enter your new password"  required>
            <label for="confirmPassword">Confirm Password:</label>
            <input type="password" id="confirmPassword" name="confirm_password" placeholder="Confirm your new password" required>
            <br>
            <button type="submit">Reset Password</button>
        </form>
    </div>


</body>
</html>
